import { Form } from "react-router-dom";

function LoginForm() {
  return <Form method="POST"></Form>;
}

// type ActionPros = {
//   parameter: object;
// };

// export async function action({ request }) {
//   // const formData = await parameter.formData();
// }

export default LoginForm;
